import React from 'react';
import PropTypes from 'prop-types';

const Login = () => {
  return (
    <div>
      <button>Login</button>
    </div>
  );
};

Login.propTypes = {

};

export default Login;

